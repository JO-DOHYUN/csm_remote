# RESEARCH_DECISIONS_KO

## 이번 리서치 반영으로 바뀐 핵심
1. `AGENTS.md`는 더 짧게 유지한다.
2. 긴 절차는 `SKILL.md`로 보낸다.
3. 장기 설명과 배경은 `docs/`로 보낸다.
4. 20B는 영구 제약이 아니라 호환 옵션으로 둔다.
5. direct sensor 값은 CAN으로 위장하지 않는다.
6. mono64 시간축과 drop visibility를 최우선 기준으로 둔다.

## 의미
리서치 반영은 단순 장식이 아니라,
- 문서 길이 최적화
- 구조적 책임 분리
- 양산형 관측/저장/검증 방향
을 현재 업계 기준에 더 가깝게 만드는 데 의미가 있다.
